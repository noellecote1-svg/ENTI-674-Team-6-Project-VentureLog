/**
 * components/ui/aspect-ratio.tsx — Aspect Ratio Component (shadcn/ui)
 *
 * Maintains a consistent width-to-height ratio for media content.
 * Wraps Radix UI's AspectRatio primitive.
 * Useful for images and video embeds that need fixed proportions.
 *
 * Exports: AspectRatio
 */
import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio"

const AspectRatio = AspectRatioPrimitive.Root

export { AspectRatio }
