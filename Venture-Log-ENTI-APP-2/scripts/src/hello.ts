/**
 * scripts/src/hello.ts — Scripts Package Sanity Check
 *
 * A minimal script used to verify the @workspace/scripts package
 * is correctly configured and can be executed in the monorepo.
 * Run with: pnpm --filter @workspace/scripts run hello
 */
console.log("Hello from @workspace/scripts");
